using UnityEngine;

public class TrollAttribute : MonoBehaviour
{
    public int strong, Hp;
    public float rotateSpeed, moveSpeed,
        backRotateSpeed, backMoveSpeed, runOrBackPoint;
    private void Update()
    {

    }
}